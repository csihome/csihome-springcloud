spring:
  application:
    name: foo-service
  cloud:
    config:
      enabled: true
      failFast: true
      discovery:
        enabled: true
        # App service pointing to Config-Server bypassing Discovery-Server
        serviceId: config-server

management:
  security:
    enabled: false
