spring:
  application:
    name: foo-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      enabled: true

management:
  security:
    enabled: false
