spring:
  application:
    name: foo-service
  cloud:
    config:
      enabled: true
      failFast: true
      discovery:
        enabled: true
        serviceId: config-server

management:
  security:
    enabled: false
